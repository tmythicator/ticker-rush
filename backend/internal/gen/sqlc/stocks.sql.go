// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: stocks.sql

package sqlc

import (
	"context"
)

const getStock = `-- name: GetStock :one
SELECT symbol, name, created_at
FROM stocks
WHERE symbol = $1 LIMIT 1
`

func (q *Queries) GetStock(ctx context.Context, symbol string) (Stock, error) {
	row := q.db.QueryRow(ctx, getStock, symbol)
	var i Stock
	err := row.Scan(&i.Symbol, &i.Name, &i.CreatedAt)
	return i, err
}

const listStocks = `-- name: ListStocks :many
SELECT symbol, name, created_at
FROM stocks
ORDER BY symbol
`

func (q *Queries) ListStocks(ctx context.Context) ([]Stock, error) {
	rows, err := q.db.Query(ctx, listStocks)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Stock
	for rows.Next() {
		var i Stock
		if err := rows.Scan(&i.Symbol, &i.Name, &i.CreatedAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertStock = `-- name: UpsertStock :one
INSERT INTO stocks (symbol, name)
VALUES ($1, $2)
ON CONFLICT (symbol) DO UPDATE SET
    name = EXCLUDED.name
RETURNING symbol, name, created_at
`

type UpsertStockParams struct {
	Symbol string
	Name   string
}

func (q *Queries) UpsertStock(ctx context.Context, arg UpsertStockParams) (Stock, error) {
	row := q.db.QueryRow(ctx, upsertStock, arg.Symbol, arg.Name)
	var i Stock
	err := row.Scan(&i.Symbol, &i.Name, &i.CreatedAt)
	return i, err
}
